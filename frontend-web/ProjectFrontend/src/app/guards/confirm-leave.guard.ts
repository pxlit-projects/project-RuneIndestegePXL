/*import { CanDeactivateFn } from '@angular/router';
import { CreateUpdateComponent } from '../components/post/create-update/create-update.component';


export const confirmLeaveGuard: CanDeactivateFn<CreateUpdateComponent> = (component, currentRoute, currentState, nextState) => {
  if(component..dirty){
    return window.confirm("Are you sure you want to leave this page?");
  }else{
    return true;
  }
};*/